[pytest]
testpaths = tests
addopts = -ra --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests that hit a live DB / Redis (require docker-compose up)
    slow: tests that take > 1s
